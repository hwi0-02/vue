// EnvConfig.java (비활성화 버전)
package com.example.backend.authlogin.config;

// @Configuration  // ← 주석 처리
// @PropertySource(value = "file:.env", ignoreResourceNotFound = true)
public class EnvConfig {
    // .env 파일 로드를 spring-dotenv에 맡깁니다.
}
